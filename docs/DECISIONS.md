# Decisions and origin

## 2026-09-04 — ARC MAP is settled

User selected the combined product: a map of Arc activity and projects, narratives from evidence,
and agents launched from those narratives to investigate. ARC MAP is the destination; Hunt this
is the signature action. The preceding idea competition is closed.

The user and a coworker already inspect Arc and draw inspiration from Robinhood-chain projects.
This establishes a concrete initial workflow and user pair, not a measured active-user claim.

The user authorized design, brand and creation of a repo to start building. The repository was
initialized on main on 2026-09-04. No remote repository, public push, deployment, wallet action or
external communication was requested as part of this kickoff.

## Origin

Application code, analysis functions and visual tokens were authored in this session. No
project-specific code or assets were copied from previous products. Public dependencies are
listed in package.json and the lockfile. Token metadata and scout findings are fetched at run time
from their labeled public source; there is no seeded fake activity.

The concept was discussed earlier the same day. Exact event-start eligibility remains to be
checked against the official start time; a new git history alone is not proof of eligibility.

AI contribution: Codex authored the implementation and documentation from the user's product
direction. User direction established the combined product, target users and desired workflow.
Preserve required planning/prompt records privately and review them for personal information
before any public submission. Event rules may require spec-driven artifacts in the submitted repo.

## Brand ruling — 2026-09-04

Fresh, light-blue direction inspired by arc.io. DM Sans across all surfaces.
ARC MAP is the destination; THE HUNT is investigation; Hunt this is the CTA.
Narrative starts from data and separates observations from questions.

## Launch priority — 2026-09-04

User direction: keep an ambitious build plan; launch as fast as possible so people can track
what is happening on Arc. The launch-first sequence is in docs/ARCHITECTURE.md. A sourced Today
feed, project pages, durable changes, following and read-only hunts form the first public release.
Graph-backed AI, shared research and funded hunts deepen the same product; paid integrations
must not gate a useful tracker. The user also requested meme-friendly design, retained in BRAND.md.
This is a plan, not a claim these capabilities are built or a public deployment is authorized.
